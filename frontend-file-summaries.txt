{
  "frontend/src/App.tsx": "TanStack Router setup with all routes; RootLayout now renders `<Layout />` (no children) wrapped in AuthGuard, since Layout uses its own internal `<Outlet />`.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations (bills CRUD, party/GST, summaries, reports, autocomplete) with robust error handling — each query returns a safe default on error, has retry logic, and uses enabled flags tied to actor availability.",
  "frontend/src/utils/formatCurrency.ts": "Utility functions for formatting numbers as Indian currency with ₹ symbol using Intl.NumberFormat.",
  "frontend/src/components/AuthGuard.tsx": "Authentication guard that checks localStorage session via getIsAuthenticated; renders SimpleLoginScreen for unauthenticated users and children for authenticated users, with local state to re-render on login.",
  "frontend/src/components/LoginScreen.tsx": "Full-page login screen with Manish Infratech branding, Internet Identity login button, and security messaging.",
  "frontend/src/components/AccessDeniedScreen.tsx": "Full-page access denied screen shown to non-owner principals with their principal ID and a logout button.",
  "frontend/src/components/ProfileSetupModal.tsx": "Full-page profile setup form shown on first login to collect the owner's name before accessing the app.",
  "frontend/src/components/Layout.tsx": "App shell with sticky red header (with `no-print` class) containing MI logo, MANISH INFRATECH branding, navigation links using correct route paths (`/`, `/search`, `/summary`, `/party-summary`, `/company-report`), logout button, main `<Outlet />`, and branded footer.",
  "frontend/src/components/AddBillForm.tsx": "Bill entry form with party name, invoice number, date, amount paid, and line items table (unit as free-text input); includes GST breakdown summary, success/error feedback, and Save Bill button disabled while actor is unavailable or saving is in progress.",
  "frontend/src/components/BillResultsTable.tsx": "Responsive bill table with invoice details, amounts, and edit/delete/print actions; fixes date display by converting nanosecond timestamps (÷ 1,000,000n) to milliseconds before rendering as DD/MM/YYYY.",
  "frontend/src/components/SummaryCard.tsx": "Grid of four metric cards (Total Revenue, Total GST, CGST, SGST) using `formatINR` from the updated formatCurrency utility, with primary-color theming for the highlight card.",
  "frontend/src/components/InvoicePrintView.tsx": "Print-optimized invoice component with `partyGstNumber` prop restored as optional; renders the red header band with MI logo + MANISH INFRATECH, party name + GST number (when provided), invoice number, bill date, line items table, GST summary (Base/CGST/SGST/Round-off/Final Amount), amount-in-words, and authorised signatory footer.",
  "frontend/src/pages/AddBillPage.tsx": "Page wrapper for AddBillForm that shows a connecting spinner while `isFetching && !actor`, an error banner with refresh button when `!isFetching && !actor`, and a connected indicator when the actor is ready — no longer destructures `error` from `useActor`.",
  "frontend/src/pages/SearchBillsPage.tsx": "Search page with real-time client-side filtering; derives connection error state from `!actorFetching && !actor` and `isError` from the bills query instead of destructuring `error` from `useActor`.",
  "frontend/src/pages/SummaryPage.tsx": "Financial summary page with date range filter, aggregate cards, P&L section, bills table, and CSV download; derives error state from `!actorFetching && !actor`, `billsError`, and `plError` (all `isError` booleans) instead of destructuring `error` from `useActor`.",
  "frontend/src/pages/InvoicePrintPage.tsx": "Invoice print page using correct route param `id` from `/invoice/$id/print` and correct `/search` navigation path; shows InvoicePrintView with Print/Back buttons in a `no-print` container.",
  "frontend/src/index.css": "Global styles with Manish Infratech OKLCH color system (red primary, off-white background), Inter font, and comprehensive `@media print` rules that hide `.no-print`, `header`, `nav`, `footer`, and `.print-hide` elements, reset page margins to A4, force background color printing, and prevent table row page breaks.",
  "frontend/tailwind.config.js": "Tailwind config with Inter/Poppins fonts, brand-red color token (#c0392b), semantic OKLCH color tokens, and extended border-radius and shadow utilities.",
  "frontend/src/components/EditBillModal.tsx": "Edit bill modal with all fields pre-filled from the existing bill (including amount paid and date converted from nanoseconds); unit field is a plain free-text input, includes GST breakdown, and submits the complete BillOperation including the required amountPaid field.",
  "frontend/src/pages/PartySummaryPage.tsx": "Party summary page with date-range filter, GST inline editing, aggregate totals, and per-party View Report button; derives error state from `!actorFetching && !actor` and `isError` from the party summary query instead of destructuring `error` from `useActor`.",
  "frontend/src/pages/CompanyReportPage.tsx": "Company-specific report page with party selector, date filter, summary cards, bills table, and CSV download; derives error state from `!actorFetching && !actor` and `isError` from the report query instead of destructuring `error` from `useActor`.",
  "frontend/src/components/AutocompleteInput.tsx": "New reusable autocomplete input component that shows a filtered dropdown of suggestions as the user types, closes on selection or outside click, and supports all standard input props.",
  "frontend/src/hooks/useSimpleAuth.ts": "Simple localStorage-based authentication hook with hardcoded credentials (MANISHINFRATECH/1752); exports login, logout, isAuthenticated functions and a standalone getIsAuthenticated helper.",
  "frontend/src/components/SimpleLoginScreen.tsx": "Redesigned professional login screen with dark gradient background, red MI logo tile, white card with red header strip, uppercase-labeled username/PIN fields with red focus borders, red submit button with hover effect, and inline error display; authentication logic unchanged.",
  "frontend/src/main.tsx": "React app entry point with QueryClientProvider only; Internet Identity provider removed.",
  "frontend/src/hooks/useActor.ts": "Actor initialization hook using the existing `useInternetIdentity` + `createActorWithConfig` pattern; now returns `actor`, `isFetching`, and `error` (from `actorQuery.error`) so all pages can handle connection failures gracefully."
}